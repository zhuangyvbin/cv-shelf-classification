# 复制为 config.secrets.yaml 后填写真实密码
# 也可通过环境变量 RACK_MYSQL_PASSWORD 统一覆盖（适合 CI/容器部署）
environments:
  development:
    mysql:
      password: your_dev_password
  testing:
    mysql:
      password: your_test_password
  production:
    mysql:
      password: your_prod_password
